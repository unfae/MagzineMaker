# MagazineMaker

This is a default Flutter application scaffold generated for the MagazineMaker project.

## Getting Started

This project is a starting point for a Flutter application.

To run the project:
```bash
flutter pub get
flutter run
```

## Project Structure
- `lib/main.dart` : Main application code
- `pubspec.yaml` : Dependencies and metadata
