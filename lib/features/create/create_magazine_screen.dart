import 'package:flutter/material.dart';

class CreateMagazineScreen extends StatelessWidget {
  const CreateMagazineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Create Magazine Screen"),
    );
  }
}
